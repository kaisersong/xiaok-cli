export function getDecompositionSection() {
    return [
        "# Planning",
        "Break substantial work into verifiable steps with clear dependencies and a step for each deliverable. Inspect relevant source before editing and update progress as results arrive. Skip formal planning for simple questions or single-step work.",
    ].join('\n');
}
